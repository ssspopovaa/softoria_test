Щоб розвернути проект  потрібно виконати наступні кроки:
- виконати команду docker composer up --build
- cp .env.example .env
- добавити в .env всі необхідні змінні (DATAIMPULSE_API_BASE_URL та DATAIMPULSE_API_TOKEN)
